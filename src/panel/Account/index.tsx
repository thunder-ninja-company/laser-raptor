import Template from 'panel/Template';

export const Account: React.FC = () => {
    return (
        <Template>
            <div>{'hummmmmm'}</div>
        </Template>
    );
};

export default Account;
