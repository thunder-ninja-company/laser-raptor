import { createStyles } from '@mantine/core';

export const useStyles = createStyles(_theme => ({
    headerFooter : {
        height     : '20px',
        alignItems : 'center',
        display    : 'flex',
    },
}));
