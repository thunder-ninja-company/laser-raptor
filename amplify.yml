version: 1
frontend:
    phases:
        preBuild:
            commands:
                - npm install
        # IMPORTANT - Testing this diff command thing
        # heres a new line
        build:
            commands: []
    artifacts:
        baseDirectory: .next
        files:
            - '**/*'
    cache:
        paths:
            - node_modules/**/*
